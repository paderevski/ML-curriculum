# Orphans

Files moved here because they had no clear unit home at reorganization time.
Each entry below notes the best guess at what it is. Triage at leisure — these
can stay here indefinitely, be promoted into a unit folder if a home is
identified, or be deleted if they're truly obsolete.

## Notebooks

- **`bayes_error_assignment.ipynb`** + **`bayes_error_assignment.py`** — alternate/earlier version of the Bayes Error material that lives in `04-first-pipeline/Bayes-Error-Student.ipynb`. The `.py` is presumably an export. Verify which version is canonical and delete the loser.
- **`Distance.ipynb`** — name is suggestive of distance-metric demos (could be a k-NN warmup in 06), but the calendar doesn't reference it. Open the notebook to confirm its purpose.
- **`Studying_Logistic_Regression.ipynb`** — title fits Unit 6 but the notebook isn't on the calendar. Could be a teacher exploration file or an early draft.
- **`Gaussian-credit.ipynb`** — sits alongside `SMOTE-credit.ipynb` / `GMM-credit.ipynb` in the old `anomaly/` folder but isn't on the calendar. Possibly a pure-Gaussian (single-distribution) baseline vs. the GMM version.

## Assets

- **`acl.png`**, **`model.pth`** — top-level files at `notebooks/` root with no obvious owner. Likely leftovers from a notebook run or a one-off demo.
- **`circle_boundary_root.png`**, **`circle_boundary_from_images_dir.png`** — two different PNGs with the same base name. The first was at `notebooks/circle_boundary.png`, the second was at `notebooks/images/circle_boundary.png`. Neither is grep-referenced from any notebook.
